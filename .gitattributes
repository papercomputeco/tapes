# Generated trees: sqlc output (make generate). Marked linguist-generated so
# review tooling collapses them by default and language stats skip them.
#
# The OpenAPI contracts used to be listed here. They are no longer generated —
# each server compiles its own from its route registrations and serves it at
# GET /openapi — so there is no file to collapse.
pkg/storage/postgres/gensqlc/** linguist-generated=true
