// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: recaps.sql

package gensqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getSessionRecap = `-- name: GetSessionRecap :one
SELECT org_id, session_id, narrative, observation, turn_count, model, generated_at FROM session_recaps
WHERE org_id = $1 AND session_id = $2
`

type GetSessionRecapParams struct {
	OrgID     pgtype.UUID
	SessionID pgtype.UUID
}

// Org-scoped point read used by GET /v1/sessions/:id/recap and the generate
// handler's cache check (a recap whose turn_count matches the session is
// returned without an LLM call).
func (q *Queries) GetSessionRecap(ctx context.Context, arg GetSessionRecapParams) (SessionRecap, error) {
	row := q.db.QueryRow(ctx, getSessionRecap, arg.OrgID, arg.SessionID)
	var i SessionRecap
	err := row.Scan(
		&i.OrgID,
		&i.SessionID,
		&i.Narrative,
		&i.Observation,
		&i.TurnCount,
		&i.Model,
		&i.GeneratedAt,
	)
	return i, err
}

const upsertSessionRecap = `-- name: UpsertSessionRecap :one
INSERT INTO session_recaps (
    org_id,
    session_id,
    narrative,
    observation,
    turn_count,
    model,
    generated_at
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7
)
ON CONFLICT (org_id, session_id) DO UPDATE
SET narrative    = EXCLUDED.narrative,
    observation  = EXCLUDED.observation,
    turn_count   = EXCLUDED.turn_count,
    model        = EXCLUDED.model,
    generated_at = EXCLUDED.generated_at
RETURNING org_id, session_id, narrative, observation, turn_count, model, generated_at
`

type UpsertSessionRecapParams struct {
	OrgID       pgtype.UUID
	SessionID   pgtype.UUID
	Narrative   string
	Observation string
	TurnCount   int32
	Model       string
	GeneratedAt pgtype.Timestamptz
}

// Insert-or-replace a session's recap keyed by (org_id, session_id) — one
// recap per session, regeneration overwrites (latest wins). Every field but
// the key is replaced on conflict: a regenerated recap supersedes the prior
// narrative/observation wholesale, and turn_count/generated_at move with it.
func (q *Queries) UpsertSessionRecap(ctx context.Context, arg UpsertSessionRecapParams) (SessionRecap, error) {
	row := q.db.QueryRow(ctx, upsertSessionRecap,
		arg.OrgID,
		arg.SessionID,
		arg.Narrative,
		arg.Observation,
		arg.TurnCount,
		arg.Model,
		arg.GeneratedAt,
	)
	var i SessionRecap
	err := row.Scan(
		&i.OrgID,
		&i.SessionID,
		&i.Narrative,
		&i.Observation,
		&i.TurnCount,
		&i.Model,
		&i.GeneratedAt,
	)
	return i, err
}
